
  # Strokes with recommendation

  This is a code bundle for Strokes with recommendation. The original project is available at https://www.figma.com/design/0CpgMHop3qm8pTQSTpfYzy/Strokes-with-recommendation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  